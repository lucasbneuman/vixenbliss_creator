# backend/.env.example.modal-sdxl-lora
# Copy this to .env and fill in your values

# ============================================================================
# Modal SDXL LoRA Configuration
# ============================================================================

# Provider selection (agnostic)
AI_IMAGE_PROVIDER=modal_sdxl_lora                       # or "serverless_http"
AI_IMAGE_PROVIDER_FALLBACKS=replicate                   # optional fallback chain
LORA_PROVIDER=modal_sdxl_lora                           # legacy alias (still supported)

# Generic HTTP provider configuration (recommended)
AI_PROVIDER_ENDPOINT_URL=https://modal-app.modal.run/generate-image
AI_PROVIDER_API_TOKEN=
AI_PROVIDER_AUTH_HEADER=Authorization
AI_PROVIDER_AUTH_SCHEME=Bearer

# Backward-compatible Modal aliases
MODAL_ENDPOINT_URL=https://modal-app.modal.run/generate-image
MODAL_API_TOKEN=
MODAL_TIMEOUT_SECONDS=300                               # Max time to wait for response

# ============================================================================
# Cloudflare R2 Configuration (for presigned LoRA URLs)
# ============================================================================

R2_ACCESS_KEY_ID=<your-r2-access-key>
R2_SECRET_ACCESS_KEY=<your-r2-secret-key>
R2_BUCKET_NAME=vixenbliss-creator
R2_ENDPOINT_URL=https://1b25234e908e6f431171a26744700241.r2.cloudflarestorage.com
R2_PUBLIC_URL=https://pub-a8fbe8295aac44d384c00952267b9f54.r2.dev

# ============================================================================
# Database Configuration
# ============================================================================

DATABASE_URL=postgresql://user:password@host:5432/vixenbliss

# ============================================================================
# Optional Fallback Providers
# ============================================================================

# If Modal fails, fallback to Replicate
REPLICATE_API_TOKEN=<your-replicate-token>
FACE_PROVIDER_ORDER=modal_sdxl_lora,replicate_sdxl,leonardo,dall_e_3

# ============================================================================
# Other Configuration
# ============================================================================

ENVIRONMENT=development
DEBUG=true
LOG_LEVEL=INFO
