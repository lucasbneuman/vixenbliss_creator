# S2 Image

Servicio neutral para generacion de imagenes de contenido.

Comparte base de runtime con `S1 image`, pero cambia workflow y politicas de consumo de LoRA.

Proveedor por defecto recomendado:

- activo: `Modal`
